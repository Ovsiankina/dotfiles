return {
  "lukas-reineke/indent-blankline.nvim",
  main = "ibl",
  opts = {},
--          ╭─────────────────────────────────────────────────────────╮
--          │     The scope is not the current indentation level!     │
--          │  Instead, it is the indentation level where variables   │
--          │                    or functions are                     │
--          │  accessible, as in Wikipedia Scope (Computer Science).  │
--          │ This depends on the language you are writing. For more  │
--          │                       information                       │
--          │               see :help ibl.config.scope.               │
--          ╰─────────────────────────────────────────────────────────╯
}
